<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div
      class="bg-zinc-900 min-h-screen w-full font-[Gilroy] text-white flex items-center justify-center"
    >
      <form
        action="/register"
        method="post"
        class="h-[60vh] md:h-[70vh] w-full md:w-[26vw] bg-zinc-800 flex items-center justify-start flex-col"
      >
        <h2 class="text-2xl text-yellow-300 mb-4 pt-3">Create New Account</h2>
        <input
          class="px-3 py-2 mb-1 bg-transparent border-2 border-zinc-700 rounded outline-none"
          type="text"
          name="name"
          placeholder="name"
        />
        <input
          class="px-3 py-2 mb-1 bg-transparent border-2 border-zinc-700 rounded outline-none"
          type="text"
          name="username"
          placeholder="username"
        />
        <input
          class="px-3 py-2 mb-1 bg-transparent border-2 border-zinc-700 rounded outline-none"
          type="number"
          name="age"
          placeholder="age"
        />
        <input
          class="px-3 py-2 mb-1 bg-transparent border-2 border-zinc-700 rounded outline-none"
          type="email"
          name="email"
          placeholder="email"
        />
        <input
          class="px-3 py-2 mb-1 bg-transparent border-2 border-zinc-700 rounded outline-none"
          type="password"
          name="password"
          placeholder="password"
        />
        <button class="bg-yellow-300 font-semibold text-zinc-700 px-2 py-2 rounded mt-3">
          Create Account
        </button>
        <h2 class="text-zinc-400 mt-2">
          Already a user? <a class="underline" href="/login">Login</a>
        </h2>

      </form>
    </div>
  </body>
</html>
